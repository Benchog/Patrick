# Copy to .env.local for local overrides (not required for GitHub/Vercel deploy)
# Optional: point the assistant at a different chat API during development
# VITE_CHAT_API_URL=http://localhost:3000/api/chat
